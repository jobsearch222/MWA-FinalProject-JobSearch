// module.exports.checkBody= (req,res,next)=>{
  
//     const student = req.body;
//      if (!student.id || !student.name || !student.course || !student.grade) {
//             res.send('Error wrong format');
//         }
//       return  next(); 
// };